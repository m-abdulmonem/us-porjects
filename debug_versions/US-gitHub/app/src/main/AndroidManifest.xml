<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.us.abuabdo.us">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/dark_blue"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/dark_blue"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".Auth.ForgetPassword"></activity>
        <activity android:name=".Auth.Register" />
        <activity android:name=".Auth.Login" />
        <activity
            android:name=".Home"
            android:label="@string/title_activity_home" />
        <activity android:name=".Welcome">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>